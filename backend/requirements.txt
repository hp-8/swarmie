# ===========================================
# Swarmie Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
#
# The roast pipeline (api/roast.py + services/swarm/*) is a pure async LLM
# fan-out. The legacy MiroFish OASIS/Zep deep-sim stack has been removed, so
# camel-ai / camel-oasis / zep-cloud are no longer needed.
# ===========================================

# Core framework
flask>=3.0.0
flask-cors>=6.0.0
gunicorn>=21.2.0

# LLM (OpenAI-format client)
openai>=1.0.0

# Deck PDF parsing
PyMuPDF>=1.24.0

# Encoding detection for non-UTF-8 text input
charset-normalizer>=3.0.0
chardet>=5.0.0

# Env + validation
python-dotenv>=1.0.0
pydantic>=2.0.0
