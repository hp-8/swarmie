# Slim production deps for the roast pipeline only.
# OASIS/Zep/camel-ai are deliberately excluded — the fast founder-validation
# flow (api/roast.py + services/swarm/*) is a pure async LLM fan-out and
# does not import them. Keeps free-tier build + memory within limits.

flask>=3.0.0
flask-cors>=6.0.0
flask-limiter>=3.5.0
gunicorn>=21.2.0

openai>=1.0.0

PyMuPDF>=1.24.0
charset-normalizer>=3.0.0
chardet>=5.0.0

python-dotenv>=1.0.0
pydantic>=2.0.0

# Roast job store — persists jobs/SSE across worker restarts when REDIS_URL set
redis>=5.0.0
